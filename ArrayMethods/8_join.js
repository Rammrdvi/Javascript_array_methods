/* Joins all elements of an array into a string. */

let arr = ['R','a','m','j','i'];

let joined = arr.join(""); //output Ramji

let joined2 = arr.join(); //output R,a,m,j,i

console.log(joined);